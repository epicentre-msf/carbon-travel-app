#Constants 

date_intervals <- c( "Month" = "month", "Quarter" = "quarter", "Year" = "year")

# Groups
group_vars <- c(
  "Organisation" = "org",
  "Reasons for Travel" = "reason_travel",
  "Flight cat" = "distance_km_cat"
)
time_serie_var <- c( "Emissions" = "emission",
                     "Expenses" = "gross_amount",
                     "Flights" = "n_flights", 
                     "Distance (Km)" = "dist_km", 
                     "Distance (Miles)" = "dist_miles")

